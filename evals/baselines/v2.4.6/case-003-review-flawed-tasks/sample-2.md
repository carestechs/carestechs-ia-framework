# Task List Review: FEAT-001 Task Labels

**Task list:** input/tasks/FEAT-001-tasks.md · **Work item:** input/docs/work-items/FEAT-001-task-labels.md
**Reviewed:** 2026-07-15 · **validate-tasks:** 8 tasks, 0 errors, 1 warning (coverage table has 5 rows but the work item defines 6 acceptance criteria) · **validate-specs:** 6 shards, 0 errors, 0 warnings

All six rubric points were examined. Sizing (point 5) produced no findings — no task spans multiple `Type` values and none is effectively larger than XL.

## Verdict

**revise** — R-1 is a CONFIRMED high-severity out-of-scope task built on an invented endpoint (T-005's `GET /api/v1/labels/export` appears in no impact table and serves no acceptance criterion), and R-2 is a CONFIRMED high-severity ordering break (T-002 integrates against endpoints that no dependency delivers). Four further CONFIRMED medium findings (R-3–R-6) independently require revision under the Step 4 rules.

## Findings

| ID | Severity | Class | Task(s) | Finding | Required Change |
|----|----------|-------|---------|---------|-----------------|
| R-1 | high | CONFIRMED | T-005, T-007 | T-005 adds `GET /api/v1/labels/export` (CSV usage analytics). This is scope creep on an invented reference: work item §4.1 (Included) lists no analytics or export capability, §7 defines exactly five new label endpoints plus the modified board list — no export endpoint is declared `(new)` anywhere — and T-005's rationale cites no AC ("Usage data will help teams…" is a new goal, not a requirement). The creep propagates into T-007, which depends on T-005 and carries the AC "Export CSV shape and counts covered". | Delete T-005 entirely; remove T-005 from T-007's Dependencies and drop the export-CSV acceptance criterion from T-007's list and description. |
| R-2 | high | CONFIRMED | T-002 | T-002's acceptance criteria require the create/rename/recolor/delete flows to "work against the label endpoints" (`GET`/`POST /api/v1/projects/{projectId}/labels`, `PUT`/`DELETE /api/v1/labels/{id}`), but its Dependencies list only T-001 (database). T-004 — the task that implements every one of those endpoints — is not a dependency, so the DAG permits T-002 to be scheduled and "completed" before any endpoint exists, an unbuildable intermediate state (rubric 4: API before frontend integration). | Add T-004 to T-002's Dependencies. |
| R-3 | medium | CONFIRMED | — | The Acceptance Criteria Coverage table lists 5 of the work item's 6 ACs — AC-5 (delete label after confirmation stating the affected-task count, work item §5) has no row. Evidence: validate-tasks warning at line 202 ("coverage table has 5 rows but the work item defines 6 acceptance criteria"). AC-5 is in fact implemented (T-002 delete confirmation, T-004 DELETE endpoint, T-008 delete-confirmation test), so this is a defect in the mandatory coverage table, not an unimplemented AC. | Add the missing row: `AC-5: delete label removes it from all tasks after confirmation → T-002, T-004, T-008`. |
| R-4 | medium | CONFIRMED | T-003, T-006 | T-003 is `Type: Backend`, but it is frontend work in full: every file it touches is React UI (`src/ui/components/label-chip.tsx`, `src/ui/components/task-card.tsx`, `src/ui/task-detail-panel.tsx`, `src/ui/project-board.tsx` — CLAUDE.md's Project Structure maps `src/ui/` to screens and `src/ui/components/` to shared React components) and all three ACs are UI behaviors. The section layout mirrors the mislabel: T-003 sits under the `## Backend` heading, as does T-006 despite T-006 being `Type: Frontend`. | Change T-003's `Type` to `Frontend`, and move T-003 and T-006 under the `## Frontend` section heading. |
| R-5 | medium | CONFIRMED | T-001, T-002, T-003, T-004 | The work item's impact sections mandate spec-index updates that no task's Files to Modify/Create includes: §6 — "add their Module Ownership and Relationships Overview entries to `docs/data-model/index.md`" (T-001 lists only the two new shards; its AC mentions indexing but the index file is absent from its Files); §7 — "add its rows to the Endpoint Summary in `docs/api-spec/index.md`" (T-004 omits it); §8 — "add its Screen Inventory row to `docs/ui-specification/index.md`" (T-002 omits it) and "LabelChip goes in `docs/ui-specification/components.md`" (T-003 omits it). Each index declares itself the shard directory, so new shards left unindexed break that contract and will surface as cross-ref errors in later `validate-specs` runs. | Add the required doc files: `docs/data-model/index.md` to T-001, `docs/ui-specification/index.md` to T-002, `docs/ui-specification/components.md` to T-003, and `docs/api-spec/index.md` to T-004 — and reflect the index/inventory updates in each task's acceptance criteria. |
| R-6 | medium | CONFIRMED | T-006 | Work item §9's edge case "Label filter active while the filtered label is deleted by another user — board refetch clears the filter and shows a notice" is addressed nowhere: §13 note 6 requires every §9 edge case to be handled as a dedicated task or as acceptance criteria in a related task, and neither T-006 (the filter task) nor any other task mentions clearing a stale filter or showing a notice. | Add an acceptance criterion to T-006: when a board refetch finds the active filter's label no longer exists, the filter is cleared and a notice is shown. |
| R-7 | medium | PLAUSIBLE | T-002, T-004 | T-002's delete confirmation must state "how many tasks are affected" before calling `DELETE /api/v1/labels/{id}`, but no task defines where that count comes from: §7's `GET /api/v1/projects/{projectId}/labels` is specified only as "List labels for a project", and T-004's ACs never mention per-label usage counts. An implementer would have to invent the data source. Judgment call — the count could plausibly be absorbed into T-004's list endpoint, but nothing currently requires it. | In T-004, specify that the label list response includes a per-label task count (e.g., a `taskCount` field, documented in `docs/api-spec/endpoints/labels.md`), and have T-002 reference it for the confirmation dialog. |
| R-8 | low | PLAUSIBLE | T-003 | T-003 is `Workflow: standard` yet visibly reshapes two user-facing screens (chip row + overflow indicator on board cards, a new Labels picker on the detail panel). The work item ties `mockup-first` only to the new Label Management Dialog (§8), and both screens are marked Modified, so `standard` is defensible — advisory only. | Consider reclassifying T-003 as `mockup-first` if the chip and picker visuals warrant design sign-off before implementation. |

## Required Changes Before Implementation

- [ ] R-1: Delete T-005 (`GET /api/v1/labels/export` is out of scope and undeclared); remove T-005 from T-007's Dependencies and drop T-007's export-CSV acceptance criterion.
- [ ] R-2: Add T-004 to T-002's Dependencies so the label endpoints exist before the dialog integrates against them.
- [ ] R-3: Add the missing AC-5 row to the Acceptance Criteria Coverage table (`AC-5 → T-002, T-004, T-008`).
- [ ] R-4: Change T-003's `Type` to `Frontend`; move T-003 and T-006 under the `## Frontend` section heading.
- [ ] R-5: Add the mandated spec-index updates to Files to Modify/Create — `docs/data-model/index.md` (T-001), `docs/ui-specification/index.md` (T-002), `docs/ui-specification/components.md` (T-003), `docs/api-spec/index.md` (T-004).
- [ ] R-6: Add a T-006 acceptance criterion covering the deleted-while-filtered edge case (refetch clears the filter and shows a notice).

PLAUSIBLE findings R-7 and R-8 remain advisory; the implementer decides.
