# Task List Review: FEAT-001 Task Labels

**Task list:** input/tasks/FEAT-001-tasks.md · **Work item:** input/docs/work-items/FEAT-001-task-labels.md
**Reviewed:** 2026-07-15 · **validate-tasks:** 8 tasks, 0 errors, 1 warning (line 202 — coverage table has 5 rows but the work item defines 6 acceptance criteria) · **validate-specs:** 6 shards, 0 errors, 0 warnings

All six rubric points were examined. Loaded shards carry fresh stamps (2026-07-14, commit `fixture1`) and validate-specs is clean, so the shards are treated as authoritative.

## Verdict

**revise** — R-1 is a CONFIRMED high-severity scope/reference violation: T-005 adds an analytics export endpoint that appears nowhere in the work item's Included scope or API impact table, and T-007 tests it. R-2 is a CONFIRMED high-severity ordering break: T-002 integrates against the five label endpoints but does not depend on T-004, which implements them. Four further CONFIRMED medium findings (R-3 through R-6) independently require revision.

## Findings

| ID | Severity | Class | Task(s) | Finding | Required Change |
|----|----------|-------|---------|---------|-----------------|
| R-1 | high | CONFIRMED | T-005, T-007 | T-005 adds `GET /api/v1/labels/export` (CSV usage analytics). This is out of scope and the endpoint is invented: work item §4.1 (Included) lists no analytics/export capability, §7 defines exactly five new label endpoints plus the modified board list — no export route — and no AC references it (T-005 maps to no row in the coverage table; its rationale cites adoption analysis, not the work item). T-007 propagates the creep: it depends on T-005 and its third AC covers the export CSV. Rubric points 2 (scope fidelity) and 3 (reference reality). | Delete T-005 entirely. In T-007: remove the dependency on T-005 and drop the "Export CSV shape and counts covered" acceptance criterion. |
| R-2 | high | CONFIRMED | T-002 | T-002's description and ACs integrate the dialog against all label endpoints (`GET`/`POST /api/v1/projects/{projectId}/labels`, `PUT`/`DELETE /api/v1/labels/{id}` — "flows work against the label endpoints"), but its only dependency is T-001 (database). The endpoints are implemented in T-004, which is not a dependency, so the order permits building T-002 before any endpoint exists — its ACs are unsatisfiable at that point. Violates rubric point 4 and the work item's generation rule §13.4 (API tasks before frontend integration tasks). | Add T-004 to T-002's Dependencies. |
| R-3 | medium | CONFIRMED | — | The mandatory Acceptance Criteria Coverage table omits AC-5 (delete label with confirmation stating affected-task count). Validator warning at line 202: "coverage table has 5 rows but the work item defines 6 acceptance criteria"; the table lists AC-1, AC-2, AC-3, AC-4, AC-6 only. AC-5 is in fact implemented (T-002 delete confirmation AC; T-004 DELETE endpoint), so the defect is the missing traceability row, not missing coverage. Rubric point 1. | Add the row `AC-5: delete removes label from all tasks after confirmation | T-002, T-004` to the coverage table. |
| R-4 | medium | CONFIRMED | T-003, T-006 | T-003 is typed **Backend** but is entirely frontend work: it renders chips and a multi-select picker, and every file it touches is React UI (`src/ui/components/label-chip.tsx`, `src/ui/components/task-card.tsx`, `src/ui/task-detail-panel.tsx`, `src/ui/project-board.tsx` — frontend per CLAUDE.md's project structure). Both T-003 and T-006 (correctly typed Frontend) sit under the `## Backend` section heading. Rubric points 5/6 (type classification). | Change T-003's Type to Frontend and move T-003 and T-006 out of the `## Backend` section into `## Frontend`. |
| R-5 | medium | CONFIRMED | T-001, T-002, T-003, T-004 | The spec index/inventory updates the work item mandates are missing from the Files to Modify/Create lists: §6 requires Module Ownership and Relationships entries in `docs/data-model/index.md` (T-001 lists only the two new shards, though its AC says "indexed"); §7 requires Endpoint Summary rows in `docs/api-spec/index.md` (absent from T-004); §8 requires a Screen Inventory row in `docs/ui-specification/index.md` (absent from T-002) and the LabelChip entry in `docs/ui-specification/components.md` (absent from T-003). As written, new shards would be created without index rows, which `validate-specs.py` cross-ref checks would later flag. Rubric point 3. | Add `docs/data-model/index.md` to T-001, `docs/ui-specification/index.md` to T-002, `docs/ui-specification/components.md` to T-003, and `docs/api-spec/index.md` to T-004. |
| R-6 | medium | CONFIRMED | T-006, T-008 | Work item edge case §9 ("Label filter active while the filtered label is deleted by another user — board refetch clears the filter and shows a notice") is addressed by no task: T-006's ACs cover only select/clear/zero-match, and T-008 tests only select and clear. §13.6 requires every §9 edge case to be addressed as a task or an AC; the omission is directly verifiable across all eight tasks. Rubric point 1. | Add an AC to T-006: on board refetch, if the active filter's label no longer exists, clear the filter and show a notice; add matching coverage to T-008. |
| R-7 | medium | PLAUSIBLE | T-002, T-004 | AC-5 requires the delete confirmation to state how many tasks are affected, but no task specifies where that count comes from: §7's five endpoints return no per-label usage count, T-004's ACs don't add one, and T-002 just "shows a confirmation stating the number of affected tasks". The data source is unspecified, so the implementer must invent contract shape mid-task. Judgment call — the count could be smuggled into the list response without a spec change, but nothing says so. | In T-004, specify the count source (e.g., include a per-label task-usage count in the `GET /api/v1/projects/{projectId}/labels` response and document it in the labels shard); reference it in T-002's confirmation AC. |
| R-8 | medium | PLAUSIBLE | T-003 | T-003 is classified `standard`, but it builds the **new** LabelChip component (§8: New) and adds a new picker UI to two user-facing screens; §8's note says frontend tasks related to the new UI "should be classified mockup-first". Judgment call: both touched screens are marked Modified, not New, so `standard` is defensible under a strict reading of the workflow rule — but the chip/picker visual surface is exactly what mockup-first de-risks. Rubric point 6. | Reclassify T-003 as `mockup-first`, or record why `standard` suffices (chips/picker rendered per the components.md LabelChip spec once T-002's mockup settles the visual language). |

## Required Changes Before Implementation

- [ ] R-1: Delete T-005 (out-of-scope `GET /api/v1/labels/export`); remove T-007's dependency on T-005 and its export-CSV acceptance criterion
- [ ] R-2: Add T-004 to T-002's Dependencies so the dialog is built after the label endpoints exist
- [ ] R-3: Add the missing AC-5 row to the Acceptance Criteria Coverage table (covered by T-002, T-004)
- [ ] R-4: Retype T-003 as Frontend and move T-003/T-006 under the `## Frontend` section
- [ ] R-5: Add the mandated spec index/inventory files to T-001 (`docs/data-model/index.md`), T-002 (`docs/ui-specification/index.md`), T-003 (`docs/ui-specification/components.md`), and T-004 (`docs/api-spec/index.md`)
- [ ] R-6: Add the §9 "filtered label deleted by another user" behavior as an AC on T-006 with test coverage in T-008

R-7 and R-8 are PLAUSIBLE advisory findings; they do not block approval but should be resolved during regeneration since T-002/T-004 (R-7) and T-003 (R-8) are already being edited.
