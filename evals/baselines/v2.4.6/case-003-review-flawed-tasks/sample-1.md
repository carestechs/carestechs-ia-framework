# Task List Review: FEAT-001 Task Labels

**Task list:** input/tasks/FEAT-001-tasks.md · **Work item:** input/docs/work-items/FEAT-001-task-labels.md
**Reviewed:** 2026-07-15 · **validate-tasks:** 0 errors, 1 warning (`FEAT-001-tasks.md:202: coverage table has 5 rows but the work item defines 6 acceptance criteria`) · **validate-specs:** 0 errors, 0 warnings (6 shards checked)

## Verdict

**revise** — R-1 is a CONFIRMED high-severity out-of-scope task: T-005 adds an analytics export endpoint that appears nowhere in the work item's Included scope (§4.1) or API impact table (§7). R-2 is a CONFIRMED high-severity ordering break: T-002 integrates against the five label endpoints but does not depend on T-004, which implements them. Five further CONFIRMED medium findings (missing AC-5 coverage row per the validator warning, T-003 mistyped Backend, mandated spec-index updates absent, an unaddressed §9 edge case, and missing repository unit tests) independently require revision.

## Findings

| ID | Severity | Class | Task(s) | Finding | Required Change |
|----|----------|-------|---------|---------|-----------------|
| R-1 | high | CONFIRMED | T-005, T-007 | T-005 adds `GET /api/v1/labels/export` (CSV usage analytics). The work item's Included scope (§4.1) lists five label capabilities — analytics/export is not one of them — and the API impact table (§7) defines exactly six endpoint rows, none of them an export route; the work item's Usage Notes rule 1 forbids tasks outside §4.1. The task's own rationale ("usage data will help teams decide which labels to keep") cites no AC. T-007 then bakes the out-of-scope endpoint into the test suite ("Export CSV shape and counts covered") and depends on T-005. | Delete T-005. In T-007: remove the T-005 dependency and the "Export CSV shape and counts covered" acceptance criterion, and drop export coverage from its Description. |
| R-2 | high | CONFIRMED | T-002 | T-002's Description and acceptance criteria integrate against four label endpoints (`GET`/`POST /api/v1/projects/{projectId}/labels`, `PUT`/`DELETE /api/v1/labels/{id}`) — "Create, rename, recolor, and delete flows work against the label endpoints" — but its only dependency is T-001 (database). The endpoints are implemented in T-004. As ordered, the dialog's acceptance criteria are unbuildable until T-004 lands: frontend integration is sequenced before the API it consumes (rubric point 4; the generating rules require API tasks before frontend integration). | Change T-002's **Dependencies** to T-004 (which already depends on T-001). The mockup phase of its `mockup-first` workflow may start earlier, but the integration acceptance criteria require the endpoints. |
| R-3 | medium | CONFIRMED | — | The mandatory Acceptance Criteria Coverage table has no row for AC-5 (delete confirmation stating affected-task count; assignments removed). Evidence: validator warning at `FEAT-001-tasks.md:202` ("coverage table has 5 rows but the work item defines 6 acceptance criteria"); the table lists AC-1–AC-4 and AC-6 only. AC-5 is in fact exercised — T-002 has the confirmation-dialog criterion and T-004/T-001 implement the delete-removes-assignments behavior — so the defect is the missing traceability row, not an unimplemented AC. | Add the row `AC-5: delete with confirmation showing affected count | T-002, T-004` to the coverage table. Recommended alongside: add an explicit T-004 acceptance criterion that `DELETE /api/v1/labels/{id}` removes all task assignments (per §7) so AC-5's backend half is verified, not just implied. |
| R-4 | medium | CONFIRMED | T-003 | T-003 is typed **Backend**, but it is entirely frontend work: rendering label chips on board cards and building the detail-panel picker, with every listed file under `src/ui/` (`label-chip.tsx`, `task-card.tsx`, `task-detail-panel.tsx`, `project-board.tsx`) — the React screen/component tree per CLAUDE.md's project structure. It also sits under the "## Backend" section heading (as does T-006, which is correctly typed Frontend), so the grouping is misleading too. | Change T-003's **Type** to Frontend and move T-003 and T-006 under the "## Frontend" section heading. |
| R-5 | medium | CONFIRMED | T-002, T-003, T-004 | The work item mandates spec-index maintenance that no task's Files to Modify/Create includes: §7 — "add its rows to the Endpoint Summary in `docs/api-spec/index.md`" (T-004 creates `endpoints/labels.md` but never touches the index); §8 — "add its Screen Inventory row to `docs/ui-specification/index.md`" (T-002 creates the screen shard only) and "LabelChip goes in `docs/ui-specification/components.md`" (T-003 creates the component but not its inventory entry). Each index doubles as the shard directory, so omitting the rows leaves new shards unreachable and will surface as `validate-specs` errors once the shards exist. (T-001's acceptance criterion "indexed per the data-model index rules" covers §6 semantically, but `docs/data-model/index.md` is likewise absent from its files list.) | Add `docs/api-spec/index.md` (Endpoint Summary rows for the five new endpoints) to T-004; `docs/ui-specification/index.md` (Screen Inventory row) to T-002; `docs/ui-specification/components.md` (LabelChip entry) to T-003; and `docs/data-model/index.md` to T-001's files list. |
| R-6 | medium | CONFIRMED | T-006 | Work-item edge case (§9) "Label filter active while the filtered label is deleted by another user — board refetch clears the filter and shows a notice" is addressed by no task. T-006's acceptance criteria cover select, clear, and zero-match only; the work item's Usage Notes rule 6 requires every §9 edge case to be addressed as a task or an acceptance criterion. This behavior (clear filter + notice on refetch) will not emerge by default. | Add an acceptance criterion to T-006: when the active filter's label no longer exists after a refetch, the filter clears and a notice is shown; add matching coverage to T-008. |
| R-7 | medium | CONFIRMED | T-007 | CLAUDE.md convention 6 requires "every repository gets a unit test against a test database." T-001 creates the `src/db/label.ts` repository (and join helpers in `src/db/task.ts`), but the testing tasks cover only API integration (T-007, Supertest) and UI (T-008) — no task creates `tests/db/label.test.ts`. | Extend T-007 with `tests/db/label.test.ts` (new) in its files list and an acceptance criterion covering label repository unit tests (uniqueness constraint, 10-label cap, cascade on delete) against the test database. |
| R-8 | medium | PLAUSIBLE | T-002, T-004 | T-002's delete confirmation must state "how many tasks are affected" (AC-5), but no defined endpoint supplies a per-label task count: §7's `GET /api/v1/projects/{projectId}/labels` is described only as "List labels for a project," and T-004's criteria add counts nowhere. The work item itself does not specify the count's source, so this is a design gap the task list should have surfaced rather than a hallucination — a judgment call, not mechanically verifiable. | Suggested: have T-004 include a `taskCount` field per label in the list response (documented in the new `endpoints/labels.md` shard) and have T-002 consume it for the confirmation dialog. |

Rubric points with no findings: sizing (point 5 — no task spans multiple Types once R-4 is fixed, and nothing exceeds XL; T-004's five endpoints at L is the upper bound but defensible) and workflow classification (point 6 — T-002 is correctly `mockup-first` for the new Label Management Dialog per §8; the remaining tasks touch only modified screens, so `standard` is consistent with the work item's mandate).

## Required Changes Before Implementation

- [ ] R-1: Delete T-005 (out-of-scope `GET /api/v1/labels/export`); strip the export acceptance criterion, description mention, and T-005 dependency from T-007.
- [ ] R-2: Change T-002's Dependencies from T-001 to T-004 so the dialog's endpoint integration follows the API implementation.
- [ ] R-3: Add the AC-5 row to the Acceptance Criteria Coverage table (`T-002, T-004`); add a T-004 acceptance criterion that label deletion removes all task assignments.
- [ ] R-4: Retype T-003 as Frontend and move T-003/T-006 under the "## Frontend" section heading.
- [ ] R-5: Add the mandated index/inventory updates to the files lists — `docs/api-spec/index.md` (T-004), `docs/ui-specification/index.md` (T-002), `docs/ui-specification/components.md` (T-003), `docs/data-model/index.md` (T-001).
- [ ] R-6: Add the deleted-while-filtered edge-case acceptance criterion to T-006 and its test coverage to T-008.
- [ ] R-7: Add label repository unit tests (`tests/db/label.test.ts`) to T-007 per CLAUDE.md convention 6.

R-8 (PLAUSIBLE) stays advisory: the affected-task-count data source should be decided during revision, but the concrete design (count field vs. dedicated endpoint) is the generator's call.
