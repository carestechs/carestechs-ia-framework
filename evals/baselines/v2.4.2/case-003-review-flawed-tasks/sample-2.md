# Task List Review: FEAT-001 Task Labels

**Task list:** input/tasks/FEAT-001-tasks.md · **Work item:** input/docs/work-items/FEAT-001-task-labels.md
**Reviewed:** 2026-07-15 · **validate-tasks:** 8 tasks, 0 errors, 1 warning (line 202: coverage table has 5 rows but the work item defines 6 acceptance criteria) · **validate-specs:** 6 shards checked, 0 errors, 0 warnings

## Verdict

**revise** — R-1 is a CONFIRMED high-severity out-of-scope task: T-005 adds a CSV analytics export endpoint that appears nowhere in the work item's Included scope or API impact table, and T-007 tests it. R-2 is a CONFIRMED high-severity ordering break: T-002 integrates the management dialog against the label endpoints but does not depend on T-004, which implements them. Four further CONFIRMED medium findings (R-3–R-6) independently require revision.

All six rubric points were examined. Sizing (point 5) produced no findings — no task exceeds XL or spans multiple Type values — and workflow classification (point 6) is correct where checked: T-002 is `mockup-first` per the work item §8 note, and the remaining tasks are appropriately `standard`.

## Findings

| ID | Severity | Class | Task(s) | Finding | Required Change |
|----|----------|-------|---------|---------|-----------------|
| R-1 | high | CONFIRMED | T-005, T-007 | T-005 adds `GET /api/v1/labels/export` (CSV usage analytics). This capability is not in the work item's Included scope (§4.1), and the API impact table (§7) defines exactly five new label endpoints plus the modified board list — "New endpoints required: All five label endpoints above" — so the endpoint is invented, not flagged as declared-new (rubric points 2 and 3). T-005's rationale ("usage data will help teams decide which labels to keep") cites no AC. It also re-marks `src/api/labels.ts` and `docs/api-spec/endpoints/labels.md` as `(new)` although T-004 already creates both. T-007 inherits the creep: its description and third AC cover the export CSV, and it depends on T-005. | Delete T-005 entirely. In T-007: remove the export CSV from the description and acceptance criteria, and drop T-005 from Dependencies (leaving T-004). |
| R-2 | high | CONFIRMED | T-002 | T-002's description wires the dialog to `GET`/`POST /api/v1/projects/{projectId}/labels` and `PUT`/`DELETE /api/v1/labels/{id}`, and its first AC requires the flows to "work against the label endpoints" — all implemented by T-004. Yet T-002's Dependencies list only T-001, so the declared order allows building the dialog's integration before any endpoint exists (rubric point 4: API before frontend integration). | Add T-004 to T-002's Dependencies. The `mockup-first` mockup stage can still precede T-004; the integration ACs cannot. |
| R-3 | medium | CONFIRMED | — | The mandatory Acceptance Criteria Coverage table has rows for AC-1–AC-4 and AC-6 but no AC-5 row (validator warning, line 202: 5 rows vs 6 ACs). AC-5 (delete removes the label from all tasks after a confirmation stating the affected count) is substantively implemented by T-002's and T-004's acceptance criteria, so this is a traceability gap in the table, not an uncovered AC (rubric point 1). | Add the row `| AC-5: delete removes label after confirmation stating affected tasks | T-002, T-004 |` to the coverage table. |
| R-4 | medium | CONFIRMED | T-003, T-006 | T-003 is typed `Backend` and grouped under the `## Backend` heading, but it is purely frontend work: it renders label chips, builds the detail-panel picker, and every file it touches lives under `src/ui/` (CLAUDE.md Project Structure: `src/ui/` and `src/ui/components/` are React screens and shared components). T-006 (correctly typed `Frontend`) is also misplaced under the `## Backend` heading. | Change T-003's Type to `Frontend` and move T-003 and T-006 under the `## Frontend` section. |
| R-5 | medium | CONFIRMED | T-006, T-008 | Work item §9 requires: "Label filter active while the filtered label is deleted by another user — board refetch clears the filter and shows a notice." No task addresses it — T-006's ACs cover only select, clear, and zero-match, and T-008 tests only those flows. The generation rules (§13.6) require every §9 edge case to be addressed as a task or as acceptance criteria within a related task (rubric point 1). | Add an AC to T-006: when the active filter's label no longer exists on refetch, clear the filter and show a notice; extend T-008's coverage to include it. |
| R-6 | medium | CONFIRMED | T-001, T-007 | CLAUDE.md convention 6 requires that "every repository gets a unit test against a test database." T-001 creates the new repository module `src/db/label.ts` (and adds join helpers to `src/db/task.ts`), but no task creates `tests/db/label.test.ts` or any repository unit test — T-007 covers only Supertest API integration tests and T-008 only UI tests. | Add repository unit-test coverage (e.g., `tests/db/label.test.ts` for uniqueness, the 10-label cap, and cascade-on-delete) to T-007's scope or as a dedicated Testing task. |
| R-7 | medium | PLAUSIBLE | T-002, T-004 | AC-5 and §9 require the delete confirmation to state how many tasks are affected, but no task specifies where the UI gets that count: §7 defines no usage-count endpoint, and T-004's ACs add labels to the board list response but no per-label task counts. As written, T-002 cannot render the number. This is a judgment call — the generator may intend the count to ride on the `GET /api/v1/projects/{projectId}/labels` response — but no task says so. | Specify the data source in T-004 (e.g., include a `taskCount` per label in the list-labels response) and reference it in T-002's delete-confirmation AC. |
| R-8 | low | PLAUSIBLE | T-001 | §6 declares TaskLabel a new entity, and the data-model index (§2) states one repository module per entity, yet T-001 creates only `src/db/label.ts` and folds TaskLabel access into `src/db/task.ts` join helpers, despite its own AC claiming "one-module-per-entity convention." For a pure join table this is defensible either way; flagging so the generator confirms intent. | Either add `src/db/task-label.ts`, or state in T-001 that TaskLabel persistence is owned by the Label/Task repositories and align the AC wording. |

## Required Changes Before Implementation

- [ ] R-1: Delete T-005 (`GET /api/v1/labels/export` is out of scope per work item §4.1/§7); remove the export CSV from T-007's description and ACs and drop T-005 from T-007's Dependencies.
- [ ] R-2: Add T-004 to T-002's Dependencies so the dialog's endpoint integration follows the API implementation.
- [ ] R-3: Add the missing AC-5 row to the Acceptance Criteria Coverage table (covered by T-002, T-004).
- [ ] R-4: Retype T-003 as `Frontend` and regroup T-003 and T-006 under the `## Frontend` section.
- [ ] R-5: Add the §9 "filtered label deleted by another user" edge case as an AC on T-006 and to T-008's test coverage.
- [ ] R-6: Add repository unit tests for the new `src/db/label.ts` module (CLAUDE.md convention 6) to T-007 or a dedicated task.

PLAUSIBLE findings R-7 and R-8 are advisory and do not block approval, though R-7 should be resolved during the R-1–R-6 revision pass since it touches the same tasks.
