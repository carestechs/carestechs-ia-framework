# Task List Review: FEAT-001 Task Labels

**Task list:** input/tasks/FEAT-001-tasks.md · **Work item:** input/docs/work-items/FEAT-001-task-labels.md
**Reviewed:** 2026-07-15
**validate-tasks:** 8 tasks, 0 errors, 1 warning — `input\tasks\FEAT-001-tasks.md:202: WARN: coverage table has 5 rows but the work item defines 6 acceptance criteria`
**validate-specs:** 6 shards checked, 0 errors, 0 warnings

## Verdict

**revise** — R-1 is a CONFIRMED high-severity scope violation: T-005 builds an analytics export endpoint that appears nowhere in the work item's Included scope or API impact table. R-2 is a CONFIRMED high-severity ordering break: T-002 integrates against the four label CRUD endpoints without depending on T-004, which implements them. Four further CONFIRMED medium findings (R-3 through R-6) also block approval.

## Findings

| ID | Severity | Class | Task(s) | Finding | Required Change |
|----|----------|-------|---------|---------|-----------------|
| R-1 | high | CONFIRMED | T-005, T-007 | T-005 adds `GET /api/v1/labels/export` (CSV usage analytics). The endpoint is invented: the work item's API impact table (§7) defines exactly five new label endpoints plus the modified board list — no export — and §4.1's Included list contains no analytics/export capability. §4.2 explicitly defers usage-data analysis ("revisit if usage data shows demand"). T-005's rationale cites no AC. It also re-marks `src/api/labels.ts` and `docs/api-spec/endpoints/labels.md` as `(new)` although T-004 already creates both. T-007 inherits the creep: its description, third AC ("Export CSV shape and counts covered"), and dependency on T-005. | Delete T-005 entirely. In T-007: remove the T-005 dependency, the export-CSV acceptance criterion, and the export mention in the description. |
| R-2 | high | CONFIRMED | T-002 | T-002's acceptance criteria require working integration ("Create, rename, recolor, and delete flows work against the label endpoints"; duplicate-name `conflict` response rendered inline), yet its Dependencies list only T-001. The four endpoints it calls are implemented by T-004 (work item §7 marks all of them New). As ordered, T-002 can be scheduled immediately after T-001 and its integration ACs are unbuildable — rubric point 4 requires API before frontend integration. Mockup-first covers the mockup phase only, not the wired flows the ACs demand. | Add T-004 to T-002's Dependencies (`Dependencies: T-001, T-004`). |
| R-3 | medium | CONFIRMED | — | The Acceptance Criteria Coverage table has no AC-5 row — confirmed by the validator warning (line 202: 5 rows vs 6 ACs) and by direct comparison with work item §5. AC-5 (delete removes label from all tasks after a confirmation stating the affected count) is in fact implemented by T-002 (confirmation dialog AC) and T-004 (DELETE endpoint) and tested by T-008, so this is a traceability gap, not missing work — but the coverage table is mandatory for feature task lists and must be complete. | Add the row `AC-5: delete removes label after confirmation → T-002, T-004, T-008` to the coverage table. |
| R-4 | medium | CONFIRMED | T-003, T-006 | T-003 ("Render label chips on board cards and the detail panel picker") is declared `Type: Backend` and sits under the `## Backend` section, but every listed file is frontend (`src/ui/components/label-chip.tsx`, `src/ui/components/task-card.tsx`, `src/ui/task-detail-panel.tsx`, `src/ui/project-board.tsx`) and the work item's §8 UI impact table owns this work (Project Board and Task Detail Panel, Modified; LabelChip, New). T-006 (`Type: Frontend`) is also grouped under `## Backend`. The wrong Type misroutes agent selection and convention checks. | Change T-003's Type to `Frontend`; move T-003 and T-006 under the Frontend section (or regroup sections to match each task's Type). |
| R-5 | medium | CONFIRMED | T-001, T-002, T-003, T-004 | Mandatory spec-index updates are missing from Files to Modify/Create. Work item §6: add Module Ownership and Relationships Overview entries to `docs/data-model/index.md` — absent from T-001, whose own AC says "written and indexed" while the file list omits the index. §7: add Endpoint Summary rows to `docs/api-spec/index.md` — absent from T-004. §8: add a Screen Inventory row to `docs/ui-specification/index.md` — absent from T-002 — and put LabelChip in `docs/ui-specification/components.md` — absent from T-003. Each index's Usage Notes (and Changelog rule) require the same updates; the index tables double as shard directories, so unindexed shards break shard routing. | Add `docs/data-model/index.md` to T-001, `docs/ui-specification/index.md` to T-002, `docs/ui-specification/components.md` to T-003, and `docs/api-spec/index.md` to T-004, each with the specific rows/changelog entries to add. |
| R-6 | medium | CONFIRMED | T-006 | Work item §9 edge case — "Label filter active while the filtered label is deleted by another user — board refetch clears the filter and shows a notice" — is covered by no task's acceptance criteria (T-006's ACs cover select, clear, and zero-match only; checked against all eight tasks). §13.6 mandates every §9 edge case be addressed as a task or an AC. | Add an AC to T-006: on board refetch, if the active filter's label no longer exists, clear the filter and show a notice; add matching coverage to T-008's filter scenarios. |
| R-7 | medium | PLAUSIBLE | T-002, T-004 | T-002 must show "a confirmation stating the number of affected tasks" (AC-5, §9), but no endpoint in §7 or T-004 exposes a per-label task count: the board list (`GET /api/v1/projects/{projectId}/tasks`) is paginated (max 100/page, boards up to 500 tasks per §10), so counting client-side is incorrect, and the label list response shape is otherwise unspecified. Judgment call on where the count belongs, but I judge this blocking: T-002's second AC has no reliable data path as specified. | In T-004, specify that the label list response (or the label DTO) includes a per-label task count, and document it in the new `docs/api-spec/endpoints/labels.md` shard; reference that count in T-002's delete-confirmation AC. |
| R-8 | low | PLAUSIBLE | T-004, T-007 | §9's concurrent-rename edge case ("last write wins; stale client receives the updated name on next board refresh") has no explicit AC or test. Plain PUT semantics deliver the behavior by default, so this is advisory, not blocking. | Add a T-007 assertion that a second PUT rename overwrites the first (last write wins) and returns the updated name. |
| R-9 | low | PLAUSIBLE | T-001 | TaskLabel is a new entity (§6) but gets no repository module: T-001 creates only `src/db/label.ts` and puts join helpers in `src/db/task.ts`, while its own AC claims the one-module-per-entity convention (CLAUDE.md convention 5; data-model index §2 maps every entity to a module). Splitting a pure join table across the two owning repositories is defensible — judgment call. | Either add `src/db/task-label.ts`, or record in the new `task-label.md` shard's Module Ownership entry that TaskLabel persistence lives in the Label and Task repositories. |

All six rubric points were examined. Point 5 (sizing) produced no findings: no task spans multiple Type values by content (T-003's mislabel is R-4), and none is effectively larger than XL — the M/L assignments are consistent with the work described.

## Required Changes Before Implementation

- [ ] R-1: Delete T-005 (out-of-scope `GET /api/v1/labels/export`); strip the export-CSV AC, description mention, and T-005 dependency from T-007.
- [ ] R-2: Add T-004 to T-002's Dependencies so the dialog's integration ACs are buildable.
- [ ] R-3: Add the missing AC-5 row to the Acceptance Criteria Coverage table (`AC-5 → T-002, T-004, T-008`).
- [ ] R-4: Change T-003's Type to `Frontend` and regroup T-003/T-006 out of the `## Backend` section.
- [ ] R-5: Add the spec-index updates to the file lists — `docs/data-model/index.md` (T-001), `docs/ui-specification/index.md` (T-002), `docs/ui-specification/components.md` (T-003), `docs/api-spec/index.md` (T-004).
- [ ] R-6: Add the deleted-label-while-filtered behavior (clear filter + notice) as an AC on T-006 and to T-008's coverage.
- [ ] R-7: Specify the per-label task-count data source in T-004 (label list response) and reference it in T-002's delete-confirmation AC — included though PLAUSIBLE because AC-5 has no reliable data path without it.
