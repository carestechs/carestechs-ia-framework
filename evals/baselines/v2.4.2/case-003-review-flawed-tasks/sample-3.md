# Task List Review: FEAT-001 Task Labels

**Task list:** input/tasks/FEAT-001-tasks.md · **Work item:** input/docs/work-items/FEAT-001-task-labels.md
**Reviewed:** 2026-07-15 · **validate-tasks:** 8 tasks, 0 errors, 1 warning (`FEAT-001-tasks.md:202: coverage table has 5 rows but the work item defines 6 acceptance criteria`) · **validate-specs:** 6 shards checked, 0 errors, 0 warnings

## Verdict

**revise** — R-1 is a CONFIRMED high-severity out-of-scope task: T-005 adds a CSV analytics export endpoint that appears nowhere in the work item's Included scope or API impact table, and T-007 tests it. R-2 is a CONFIRMED high-severity dependency gap: T-002 integrates against the five label endpoints but does not depend on T-004, which implements them. Four further CONFIRMED medium findings (missing AC-5 coverage row, T-003 mistyped as Backend, mandated spec-index updates absent from every Files list, an unaddressed §9 edge case) independently require revision.

## Findings

| ID | Severity | Class | Task(s) | Finding | Required Change |
|----|----------|-------|---------|---------|-----------------|
| R-1 | high | CONFIRMED | T-005, T-007 | T-005 invents `GET /api/v1/labels/export` (CSV of per-label usage counts). The work item's Included scope (§4.1) lists no analytics or export capability; the API impact table (§7) declares exactly five new label endpoints plus the modified board list — no export; no acceptance criterion in §5 mentions usage analytics, and T-005's own rationale cites no AC. The endpoint also contradicts the API spec: `docs/api-spec/index.md` §1.1 defines the API as "JSON only," and `GET /api/v1/labels/export` collides with the `PUT/DELETE /api/v1/labels/{id}` route shape. T-007's scope and dependencies inherit the problem (export CSV coverage, dependency on T-005). This is scope creep (rubric 2) built on an invented reference (rubric 3). | Delete T-005. In T-007: remove the T-005 dependency, the "Export CSV shape and counts covered" AC, and the export mention in the description. |
| R-2 | high | CONFIRMED | T-002 | T-002 (label management dialog) integrates directly against `GET/POST /api/v1/projects/{projectId}/labels`, `PUT/DELETE /api/v1/labels/{id}` — all declared **New** in work item §7 and implemented only by T-004 — yet its Dependencies list is `T-001` only. Nothing in the DAG orders T-002 after T-004, so T-002 can be scheduled while its endpoints do not exist; its ACs ("flows work against the label endpoints", "duplicate-name `conflict` response renders…") are unsatisfiable at that point. Work item §13 rule 4 requires API tasks before frontend integration tasks (rubric 4). | Change T-002's Dependencies to `T-001, T-004`. |
| R-3 | medium | CONFIRMED | — | The Acceptance Criteria Coverage table has rows for AC-1, AC-2, AC-3, AC-4, and AC-6 but no row for AC-5 (delete with confirmation stating the affected task count) — flagged by validate-tasks as a warning (line 202: 5 rows vs 6 ACs). AC-5 is in fact implemented (T-002's delete-confirmation AC, T-004's DELETE endpoint), so this is a traceability defect in the mandatory coverage table, not an uncovered AC (rubric 1). | Add the row `AC-5: delete removes label from all tasks after confirmation | T-002, T-004` to the coverage table. |
| R-4 | medium | CONFIRMED | T-003 | T-003 is typed **Backend** and sits under the `## Backend` section, but it is frontend work: every file in its Files list is under `src/ui/` (`label-chip.tsx`, `task-card.tsx`, `task-detail-panel.tsx`, `project-board.tsx`), and the work it describes (board card chips, detail-panel picker) is exactly what work item §8 lists as UI impact (Project Board Modified, Task Detail Panel Modified, LabelChip New). Wrong type classification (rubric 5/6). | Set T-003's Type to `Frontend` and move it under the Frontend section. |
| R-5 | medium | CONFIRMED | T-001, T-002, T-003, T-004 | The work item mandates spec index/inventory updates that appear in no task's Files to Modify/Create: §6 — add Label/TaskLabel Module Ownership and Relationships entries to `docs/data-model/index.md` (T-001 lists only the two new shards); §7 — add the label endpoint rows to the Endpoint Summary in `docs/api-spec/index.md` (T-004 omits it); §8 — add the Label Management Dialog Screen Inventory row to `docs/ui-specification/index.md` (T-002 omits it) and the LabelChip entry to `docs/ui-specification/components.md` (T-003 omits it). Each index doubles as its spec's shard directory, so new shards left unindexed break shard discovery (rubric 3). | Add `docs/data-model/index.md` to T-001, `docs/ui-specification/index.md` to T-002, `docs/ui-specification/components.md` to T-003, and `docs/api-spec/index.md` to T-004, each with the update the work item names. |
| R-6 | medium | CONFIRMED | T-006 | Work item §9 edge case "Label filter active while the filtered label is deleted by another user — board refetch clears the filter and shows a notice" is addressed by no task: T-006's ACs cover select, clear, and zero-match only. §13 rule 6 requires every §9 edge case to be addressed as a task or an AC (rubric 1). | Add an AC to T-006: on board refetch, if the active filter's label no longer exists, clear the filter and show a notice. Add matching coverage to T-008. |
| R-7 | medium | PLAUSIBLE | T-003 | T-003's Workflow is `standard`, but it builds the **New** LabelChip component (§8) and makes visible changes to two user-facing screens (chip rows on board cards, a new multi-select Labels field on the detail panel). Work item §8 says related frontend tasks should be classified `mockup-first`. Judgment call: the two screens are Modified rather than New, and §8's note is attached to the new-screen requirement, so `standard` is defensible — but the new-component visual surface leans `mockup-first`. | Reclassify T-003 as `mockup-first` (after retyping it Frontend per R-4), or record why `standard` is intended. |
| R-8 | medium | PLAUSIBLE | T-002, T-004 | T-002's delete confirmation must state "the number of affected tasks" (AC-5), but no task defines where that count comes from: §7's five endpoints and T-004's ACs specify no per-label task-count field, and no loaded shard provides one. The `labels.md` shard is (new), so T-004 *could* include a usage count in the list-labels DTO — but no AC pins it down, leaving T-002's flow unimplementable as specified. Judgment call because the contract is unspecified rather than contradicted. | Add an AC to T-004 that the label list response (or the DELETE precheck) exposes each label's assigned-task count, and reference it from T-002's confirmation AC. |
| R-9 | low | PLAUSIBLE | T-004, T-007 | §9's concurrent-rename edge case ("last write wins; stale client receives the updated name on next board refresh") is not explicitly verified anywhere. Plain `PUT` semantics likely satisfy it by default, so this may need no code — but no AC or test asserts it. | Add a T-007 test AC asserting last-write-wins on concurrent rename, or note in T-004 that default PUT semantics cover it. |
| R-10 | low | PLAUSIBLE | T-001 | TaskLabel is a declared new entity (§6) with its own shard, but T-001 creates no `src/db/task-label.ts` — join helpers go into `src/db/task.ts` — while CLAUDE.md convention 5 and `docs/data-model/index.md` §2 state one repository module per entity. Whether a pure join table warrants its own module is a judgment call; T-001's own AC ("one-module-per-entity convention") is ambiguous about it. | Either add `src/db/task-label.ts` to T-001, or state in T-001 that TaskLabel assignments are owned by the Task repository. |

## Required Changes Before Implementation

- [ ] R-1: Delete T-005 (out-of-scope `GET /api/v1/labels/export`); remove the T-005 dependency, the export-CSV AC, and the export mention from T-007.
- [ ] R-2: Add T-004 to T-002's Dependencies so the dialog is built after the label endpoints exist.
- [ ] R-3: Add the missing `AC-5 | T-002, T-004` row to the Acceptance Criteria Coverage table (resolves the validate-tasks warning).
- [ ] R-4: Retype T-003 as `Frontend` and move it under the Frontend section.
- [ ] R-5: Add the mandated spec index/inventory updates to the Files lists — `docs/data-model/index.md` (T-001), `docs/ui-specification/index.md` (T-002), `docs/ui-specification/components.md` (T-003), `docs/api-spec/index.md` (T-004).
- [ ] R-6: Add the filtered-label-deleted edge case (clear filter + notice on refetch) as an AC on T-006, with test coverage in T-008.

R-7 through R-10 are advisory (PLAUSIBLE); the implementer or regeneration session decides.
