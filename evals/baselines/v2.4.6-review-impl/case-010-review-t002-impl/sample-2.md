# Implementation Review: T-002 Label CRUD Endpoints

**Task:** T-002 (tasks/FEAT-001-tasks.md) · **Plan:** plans/plan-T-002-label-crud-endpoints.md
**Diff:** `git diff main..feat/FEAT-001-T-002-labels` (input/diff.md) · **Reviewed:** 2026-07-16
**Evidence (frozen record, input/EVIDENCE.md):** tests NOT RUNNABLE (fixture has no `package.json`; `tests/api/labels.test.ts` has never executed — test adequacy judged statically) · linters UNAVAILABLE (no lint config) · `validate-specs.py` over the merged docs: 7 shards checked, **0 errors, 1 warning** — `docs/api-spec/index.md: WARN: existing shard endpoints/labels.md is not mentioned in this index`

## Verdict

**revise** — R-1 is a CONFIRMED unmet acceptance criterion: AC-2 requires duplicate label names to return the Error Catalog's error, and the router contains no conflict handling at all — no 409 in code, shard, or tests. R-2 (out-of-scope CSV export endpoint added to the public contract, violating the envelope convention) and R-3 (plan step 8 skipped — `docs/api-spec/index.md` untouched, confirmed by the validator warning) also block; R-4 and R-5 are CONFIRMED medium.

## Findings

| ID | Severity | Class | AC/Plan Ref | Finding | Required Change |
|----|----------|-------|-------------|---------|-----------------|
| R-1 | high | CONFIRMED | AC-2 · plan steps 3–4 · index §2.5 | AC-2 requires a duplicate name within a project to return the catalog's error — `conflict` (409) per index §2.5; the plan's Edge Cases warn both create and rename must map the repository's uniqueness violation, never leak a raw driver error. The diff has no such handling: the POST and PUT handlers in `src/api/labels.ts` call `labelRepo.create`/`labelRepo.replace` with no duplicate check and no error mapping; `conflict`/409 appears nowhere in the router, and the shard's POST and PUT status tables (docs/api-spec/endpoints/labels.md) omit 409. No test covers it. | Map uniqueness violations on both create and rename paths to `ApiError` `conflict` (409) with `message` naming `name`; add 409 `conflict` rows to the shard's POST and PUT status tables; add a duplicate-name test. |
| R-2 | high | CONFIRMED | — (scope) · plan "Scope boundary" | `GET /api/v1/labels/export` (CSV dump) is scope creep: it appears in no task field and no plan step, and the plan's Scope boundary section explicitly bounds the task. It adds a public contract surface (shard frontmatter route + section, router handler, test) that violates CLAUDE.md convention 2 (returns raw `text/csv`, not the `{ "data": ... }` envelope), skips the project-existence 404 check the list route performs, and silently caps at 1000 rows. | Remove the export endpoint: the handler in `src/api/labels.ts`, the shard's frontmatter route and `GET /api/v1/labels/export` section, and the export test. If wanted, propose it as its own task. |
| R-3 | medium | CONFIRMED | Plan step 8 · index usage note 5 · rubric 5 | `docs/api-spec/index.md` is untouched by the diff: no Endpoint Summary rows for the new routes and no Changelog entry, both required by plan step 8 and the index's usage note 5. Evidence: validator warning verbatim — `docs/api-spec/index.md: WARN: existing shard endpoints/labels.md is not mentioned in this index`. | Add the new label routes to the Endpoint Summary (Section 4) pointing at `endpoints/labels.md`, and add a Changelog row for the addition. |
| R-4 | medium | CONFIRMED | Plan step 4 | Undocumented plan deviation: the plan specifies `PATCH /api/v1/labels/{id}` as a partial update (`name?`/`color?`, at least one required, mirroring `PATCH /api/v1/tasks/{id}`); the diff implements `PUT` full replace with both fields required (`labelReplaceSchema` in `src/api/labels.ts`; shard documents "PUT … both fields required"). The deviation is surfaced nowhere, and recolor-only clients must now resend the name. | Implement PATCH partial update per the plan (mirroring the tasks resource) and update the shard — or, if PUT is deliberately kept, document and justify the deviation for acceptance. |
| R-5 | medium | CONFIRMED | Rubric 6 · AC-2 · CLAUDE.md convention 6 | `tests/api/labels.test.ts` covers only success paths (list, create, replace, delete, export). Static read confirms no test asserts any error path: no duplicate-name 409, no `validation-error` 400, no `not-found` 404 — the ACs name the duplicate-name error explicitly. (Suite not runnable in this fixture per EVIDENCE.md; this is a coverage finding, not an execution failure.) | Add error-path tests: duplicate-name conflict (create and rename), invalid/empty body → 400, unknown project/label → 404. |
| R-6 | low | CONFIRMED | — (scope) · T-007 | `tests/api/labels.test.ts` is not in T-002's Files to Modify/Create nor its plan — the task list assigns that file (new) to T-007. Router tests are conventionally expected (CLAUDE.md convention 6), so this is advisory, but the overlap with T-007 is undocumented. | Note the deviation and reconcile with T-007 so the file isn't double-created; keep the tests. |
| R-7 | low | PLAUSIBLE | Shard LabelDto · CLAUDE.md convention 4 | Validation runs before normalization in `src/api/labels.ts`: a whitespace-only `name` (e.g. `" "`) passes `z.string().min(1)`, then `normalizeLabelName` reduces it to `""` — an empty stored name, violating the shard's "1–30 chars, whitespace-normalized". Cannot be executed in this fixture, so flagged as a judgment call. | Validate the normalized value (normalize first, or add a post-normalization non-empty check) on create and rename. |

Rubric points with no finding: AC-1 passes — the five label operations (list, create, rename, recolor, delete) all return the standard envelope, with `meta` on the list only; convention compliance is otherwise clean (Zod schemas beside handlers, `ApiError` throws, SQL confined to the repository, kebab-case naming).

## Required Changes Before Completion

- [ ] R-1: Reject duplicate label names on create **and** rename with the catalog's `conflict` (409), message naming `name`; add 409 rows to the shard's POST/PUT status tables; cover with a test.
- [ ] R-2: Remove the out-of-scope `GET /api/v1/labels/export` endpoint (handler, shard route + section, test); raise it as its own task if desired.
- [ ] R-3: Add the label routes to the API index's Endpoint Summary and record the addition in its Changelog (clears the validator warning).
- [ ] R-4: Restore the planned `PATCH /api/v1/labels/{id}` partial update (and shard) or document and justify the PUT deviation for acceptance.
- [ ] R-5: Add error-path tests — duplicate-name 409, `validation-error` 400, `not-found` 404.
