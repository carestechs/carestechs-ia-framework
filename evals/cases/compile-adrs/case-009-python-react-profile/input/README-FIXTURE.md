Frozen subset of the carestechs-software-architecture ADR repo, captured 2026-07-16 for eval determinism — do not sync with the live repo.
The profile copy lists exactly the 18 frozen ADRs (four Recommended entries trimmed); every `Requires:` chain is closed within this set so Step 0 validates clean.
Assertions and the reference output depend on this exact content.
